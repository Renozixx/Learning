#include <stdio.h>

int main() {
    int N, i = 0, j, aux = 0, f, pop, ver = 0;
    scanf("%d", &N);
    
    int baloes[N], k = (N/2), baloes2[N];
    
    for(i=0; i<N; i++){
        scanf("%d", &baloes[i]);
        baloes2[i] = 0;
    }
    
    while (pop != N)
    {
        aux++;
        pop++;
        if (pop == N)
        {
            break;
        }
        baloes2[i] = baloes[i];
        for(j = 0; j < N; j++)
        {
            if (baloes2[j] == 0)
            {
                break;
            } else if ({z})
            {
                
            }
        }
        i++;
    }
    printf("%d", aux);
    return 0;
}